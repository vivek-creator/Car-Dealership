// This is more of a guideline since we're not using Mongoose schemas
const User = {
    username: String,
    email: String,
    password: String,
    // Add more fields as necessary
  };
  
  module.exports = User;
  